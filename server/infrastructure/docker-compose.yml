version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: roadtrip-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
